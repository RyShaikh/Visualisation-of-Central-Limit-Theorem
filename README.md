This project demonstrates the Central Limit Theorem and shows how as sample size increases, the distribution of sample means of a random variable approaches a normal distribution, regardless of the shape of the original population distribution. I first verified this using dice rolls (a discrete distribution), then extended the simulation to an exponentially distributed random variable (a continuous) to confirm the result holds more generally.
